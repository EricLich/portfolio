export type PageLink = {
  text: string;
  link: string;
};

export type Experience = {
  timeSpan: string;
  company: string;
  title: string;
  description: string;
};

export type Technology = {
  name: string;
  imgPath: string;
}